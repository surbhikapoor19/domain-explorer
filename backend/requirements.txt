flask==3.0.0
flask-cors==4.0.0
python-dotenv>=1.0.0
pandas==2.1.4
numpy==1.26.2
scikit-learn==1.3.2
hdbscan>=0.8.33
umap-learn==0.5.5
sentence-transformers>=2.2.0
huggingface_hub>=0.20.0
chromadb>=0.5.0
PyMuPDF>=1.23.0
pyyaml>=6.0
groq>=0.4.0

# Dev / test only. Tests live in backend/tests/ and exercise the
# precompute + role-schema pipeline; they never run in production.
pytest>=8.0

# Optional: VLM-based PDF parsing with Docling (CPU or GPU)
# Uncomment to enable the "docling" parsing backend in rag_config.yaml
# docling>=2.0.0
