# Pinned CI dependencies for reproducible domain-build runs.
# Known-good versions captured from the last successful build (2026-09-02).
# torch is installed separately from the CPU wheel index in the workflow.
pandas==3.0.5
scikit-learn==1.9.0
umap-learn==0.5.12
hdbscan==0.8.44
sentence-transformers==6.0.1
numpy==2.5.2
requests==2.34.2
chromadb==1.5.9
lxml==6.1.2
beautifulsoup4==4.15.0
groq==1.7.0
pymupdf==1.28.2
keybert==0.9.0
